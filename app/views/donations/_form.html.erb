<div class="w-50">
  <%= form_for @donation do |f| %>
    <div class="form-group">
      <%= f.label :amount %>
      <%= f.number_field :amount, class: "form-control" %>
    </div>

    <%= f.submit "Donate" %>
  <% end %>
</div>
